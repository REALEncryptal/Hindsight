; Refuse to install any package version published in the last 7 days.
; Protects against freshly compromised package versions (npm supply-chain
; attacks). Requires npm >= 11.5.1. Value is in minutes — 10080 = 7 days.
minimum-release-age=10080
